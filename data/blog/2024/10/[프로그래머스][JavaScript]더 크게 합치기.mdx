---
title: '[프로그래머스][JavaScript]더 크게 합치기'
date: '2024-10-20'
tags: ['Algorithm','Coding Test','JavaScript']
draft: false
summary: ''
---
#### 문제 설명
````
연산 ⊕는 두 정수에 대한 연산으로 두 정수를 붙여서 쓴 값을 반환합니다. 예를 들면 다음과 같습니다.

12 ⊕ 3 = 123
3 ⊕ 12 = 312
양의 정수 a와 b가 주어졌을 때, a ⊕ b와 b ⊕ a 중 더 큰 값을 return 하는 solution 함수를 완성해 주세요.

단, a ⊕ b와 b ⊕ a가 같다면 a ⊕ b를 return 합니다.
````
#### 제한사항
    1 ≤ a, b < 10,000
#### 입출력 예
입출력 #1

|a	| b	|result|
|---|---|------|
|9	|91	|991|
|89	|8	|898|

---
#### 풀이
    function solution(a, b) {
        var answer = 0;    
        var sum1 = a.toString() + b.toString();
        var sum2 = b.toString() + a.toString();
        answer = sum1 &gt; sum2 ? sum1 : sum2
        return Number(answer);
    }
---
#### 다른사람 풀이 보고 알게된 것
[Standard built-in objects](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math)

[Math.max()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/max)

Number(\`${a}{b}\`)