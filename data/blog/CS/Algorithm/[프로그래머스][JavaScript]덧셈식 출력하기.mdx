---
slug: 2024/10/[프로그래머스][JavaScript]덧셈식 출력하기
title: '[프로그래머스][JavaScript]덧셈식 출력하기'
date: '2024-10-15'
tags: ['CS', 'Algorithm', 'Coding Test', 'JavaScript']
draft: false
summary: ''
---
#### 문제 설명
두 정수 a, b가 주어질 때 다음과 같은 형태의 계산식을 출력하는 코드를 작성해 보세요.
```a + b = c```


#### 제한사항
```1 ≤ a, b ≤ 100```

---
#### 입출력 예
입력 #1

>4 5

출력 #1

>4 + 5 = 9
---
#### 풀이
````
const readline = require('readline');
const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout
});

let input = [];

rl.on('line', function (line) {
    input = line.split(' ');
}).on('close', function () {
    console.log(input[0] + ' + ' + input[1] + ' = ' + (parseInt(input[0])+  parseInt(input[1])))
});
````
한줄 소감
> 원래는 console.log(input[0] + ' + ' + input[1]+' = ' + parseInt(input[0])+parseInt(input[1]))
> 
> 이렇게 했었는데 결과값이 이상했다. 
> 
> 4 + 5 = 45 
> 
> 이유를 보니, 덧셈 연산이 앞에서부터 순차적으로 실행되어서 그런것이었다...
> 
> 그냥 수학이랑 똑같지 뭐....